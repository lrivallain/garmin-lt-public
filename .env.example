# Environment variables

# Web server port
PORT=5000

# Custom title for the app (appears in header and browser tab)
# Examples: "John's LiveTrack", "Family Tracker", "Marathon Watch"
APP_TITLE=Garmin LiveTrack Public

# Gmail account that receives LiveTrack emails
# If set, the app will verify the authenticated account matches this email
# Leave empty to skip validation
GMAIL_ACCOUNT=your-email@gmail.com

# Gmail API credentials file path (default: credentials.json)
GMAIL_CREDENTIALS_FILE=credentials.json

# Gmail API token file path (default: token.json)
GMAIL_TOKEN_FILE=token.json

# Email check interval in seconds (default: 30)
EMAIL_CHECK_INTERVAL=30

# Maximum activity age in hours before marking as "Last activity" (default: 24)
# After this time, the status changes from "Activity in progress" to "Last activity"
# Set to a higher value if you want activities to remain "active" longer
ACTIVITY_MAX_AGE_HOURS=24

# Maximum activity age in hours before marking as "No recent activity" (default: 48)
# After this time, the status changes from "Last activity" to "No recent activity"
DEAD_ACTIVITY_MAX_AGE_HOURS=48